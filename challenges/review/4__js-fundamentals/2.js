// Write a function that takes a string as the argument.
// The function should return a number that is the number of characters in the string.
// Choose names that make the code easy to understand.


function numStr (str) {
  // get str length. and return str number.
  return str.length;
}

// trial case
// console.log(numStr("smile"));

// trial cases 
// let x = ["hello", "hey", "the mummy", "brendan fraser"]; // [5, 3, 9, 14]
// console.log(x.map(numStr));
